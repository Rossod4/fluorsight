node_modules
dist
*.local
.DS_Store

.tmpscript
.playwright-mcp

# Scratch files subagents sometimes drop at the repo root. Scoped to the root
# only ("/*.html", not "*.html") so real HTML in subdirectories is still tracked.
/*.html
!/index.html
/soc2.txt

# Excel lock files
~$*.xlsx
